{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -f next.config.js ]; then npm install; else npm install --prefix study-tracker; fi",
  "buildCommand": "if [ -f next.config.js ]; then npm run build; else npm run build --prefix study-tracker; fi"
}

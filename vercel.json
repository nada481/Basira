{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -f study-tracker/package.json ]; then npm install --prefix study-tracker; else npm install; fi",
  "buildCommand": "if [ -f study-tracker/package.json ]; then npm run build --prefix study-tracker; else npm run build; fi"
}
